# Copy to .env (git-ignored). docker-compose.yml reads GEMINI_API_KEY from here
# and passes it into the container for the "Process" (Gemini analysis) feature.
# Get a key at https://aistudio.google.com/apikey
GEMINI_API_KEY=your-api-key-here

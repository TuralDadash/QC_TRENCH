You are checking a fiber-optic trench installation photo submitted by a contractor.

Report what you see in the image:

- has_trench: an open dug-out channel in the ground is visible.
- has_vertical_measuring_stick: a light-grey ruler with red and black markings is standing upright in the trench (used to show depth).
- has_address_sheet: a printed sheet of paper with a street address is visible.
- address: the address text on the sheet, or null if absent or unreadable.
- has_sand_bedding: a layer of sand is visible inside the trench (typically under the duct).

Judge strictly on what is actually visible.
